
import { SiteConfig } from '../types';

export const siteConfig: SiteConfig = {
  UPLOAD_FOLDER_URL: "https://photos.app.goo.gl/ZwC5xnvfy2H4pPia8",
  WHATSAPP_JOIN_URL: "", // Set to empty to hide button as per rules
  OPTIONAL_AUDIO_PATH: "", // Local path if provided
};
